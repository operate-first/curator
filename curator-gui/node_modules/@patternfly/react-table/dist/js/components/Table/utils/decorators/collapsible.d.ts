import { IExtra, IFormatterValueType, IFormatter, decoratorReturnType } from '../../Table';
export declare const collapsible: IFormatter;
export declare const expandable: IFormatter;
export declare const expandedRow: (colSpan: number) => (value: IFormatterValueType, { rowIndex, rowData, column: { extraParams: { contentId } } }: IExtra) => decoratorReturnType;
