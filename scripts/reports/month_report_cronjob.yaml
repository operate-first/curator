apiVersion: batch/v1
kind: CronJob
metadata:
  name: monthreport
spec:
  schedule: "0 0 1 * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: monthreport
            image: busybox
            imagePullPolicy: IfNotPresent
            command:
            - psql -U user50S -d sampledb -c 'SELECT generate_reporot(\'month\');'
          restartPolicy: OnFailure
